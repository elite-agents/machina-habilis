{
  "name": "machina-habilis",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' --cached build",
    "dev": "bun run --filter '*' --cached dev",
    "test": "bun test",
    "clean": "rm -rf node_modules && rm -rf apps/*/node_modules && rm -rf packages/*/node_modules"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.5.0",
    "typescript": "^5.0.0"
  }
}
